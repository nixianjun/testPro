<?xml version="1.0" encoding="UTF-8" ?>   
    <!DOCTYPE mapper   
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ItemChangeHisDAO">
	<select id="selectLastItemChanges" parameterType="java.util.Map" resultType="com.tudou.userstat.model.TDMsg">
		select id, item_id, add_time, oper_type from td_itemchangehis_${date} order by id desc limit #{start}, #{limit}
	</select>
	<select id="selectLastCommentChanges" parameterType="java.util.Map" resultType="com.tudou.userstat.model.TDMsg">
		select id, item_id, create_time as add_time, msg_type as oper_type from td_commentchangehis_${date} order by id desc limit #{start}, #{limit}
	</select>
	<select id="selectLastSubChanges" parameterType="java.util.Map" resultType="com.tudou.userstat.model.TDMsg">
		select id, subed_user_id as uid, create_time as add_time, msg_type as oper_type from td_usersubchangehis_${date} order by id desc limit #{start}, #{limit}
	</select>
	<select id="selectLastDigChanges" parameterType="java.util.Map" resultType="com.tudou.userstat.model.TDMsg">
		select id, item_id, create_time as add_time, msg_type as oper_type from td_digchangehis_${date} order by id desc limit #{start}, #{limit}
	</select>
</mapper>